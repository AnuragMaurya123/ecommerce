{
    "version": 2,
    "builds": [
        {
            "src": "cluster.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                    "dist/**"
                ]
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "cluster.js"
        }
    ]
}